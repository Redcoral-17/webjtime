spring:
  datasource:
    url: jdbc:h2:mem:jtimedev
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop

app:
  admin:
    username: ${ADMIN_USERNAME:dev_admin}
    password: ${ADMIN_PASSWORD:changeme_dev}
